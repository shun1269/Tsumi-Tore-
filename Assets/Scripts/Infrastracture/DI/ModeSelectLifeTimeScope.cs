using VContainer;
using VContainer.Unity;

public class ModeSelectLifeTimeScope : LifetimeScope
{
    protected override void Configure(IContainerBuilder builder)
    {
        // ModeSelect専用の依存関係を登録します
    }
}
