using VContainer;
using VContainer.Unity;

public class PracticeSceneLifeTimeScope : LifetimeScope
{
    protected override void Configure(IContainerBuilder builder)
    {
        // PracticeScene専用の依存関係を登録します
    }
}
